@model TurnosApp.Models.Empleado

<h2>Registrar Empleado</h2>

<form asp-action="Create" method="post">
    <div class="form-group">
        <label asp-for="Nombre"></label>
        <input asp-for="Nombre" class="form-control" />
        <span asp-validation-for="Nombre" class="text-danger"></span>
    </div>

    <div class="form-group">
        <label asp-for="Cedula"></label>
        <input asp-for="Cedula" class="form-control" />
        <span asp-validation-for="Cedula" class="text-danger"></span>
    </div>

    <div class="form-group">
        <label>Departamento</label>
        <select id="DepartamentoId" name="DepartamentoId" class="form-control">
            <option value="">-- Seleccione un departamento --</option>
            @foreach (var d in ViewBag.Departamentos)
            {
                <option value="@d.Id">@d.Nombre</option>
            }
        </select>
    </div>

    <div class="form-group">
        <label>Posición</label>
        <select id="PosicionId" name="PosicionId" class="form-control">
            <option value="">-- Seleccione un cargo --</option>
        </select>
    </div>

    <button type="submit" class="btn btn-success">Guardar</button>
</form>

<script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
<script>
    $('#DepartamentoId').change(function () {
        var deptId = $(this).val();
        $('#PosicionId').empty().append('<option value="">Cargando...</option>'); // Display loading message
        $.get(`/Empleado/GetPosiciones?departamentoId=${deptId}`, function (data) {
            $('#PosicionId').empty().append('<option value="">-- Seleccione un cargo --</option>'); // Clear previous options
            data.forEach(function (p) {
                $('#PosicionId').append(`<option value="${p.id}">${p.nombre}</option>`); // Populate new positions
            });
        });
    });
</script>
